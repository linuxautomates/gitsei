# happy-mock

## Mappings

All the mappings are in the mappings folder. It is json structure and pretty self explainatory

## Running WireMock

1. Checkout the repo
2. cd into the repo
3. execute command java -jar wiremock-standalone-2.23.2.jar 
