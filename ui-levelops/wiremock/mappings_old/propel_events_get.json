{
  "request": {
    "method": "GET",
    "urlPathPattern": "/v1/events/types/(.*)"
  },
  "response": {
    "status": 200,
    "jsonBody": {
      "type": "PLUGIN_RESULT_CREATED",
      "component_type": "PLUGIN_RESULT",
      "description": "Event triggered when a new plugin result is created.",
      "data": [
        {
          "key": "product",
          "type": "multi-dynamic-select",
          "required": true,
          "validation": "not_empty",
          "dynamic_resource_name": "products"
        }
      ]
    },
    "headers": {
      "Content-Type": "application/json",
      "Access-Control-Allow-Origin": "*"
    }
  }
}
