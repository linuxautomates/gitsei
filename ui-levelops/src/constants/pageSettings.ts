export const DEFAULT_PAGE_SIZE = 50;

// key used in table column config that decide if we want to call API on sort change with new value
// false => will not call the API
// else by default API will be called
export const API_CALL_ON_TABLE_SORT_CHANGE = "api_call_on_table_sort_change";
