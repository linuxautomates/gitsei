export enum WIDGET_CONFIGURATION_KEYS {
  FILTERS = "filters",
  METRICS = "metrics",
  AGGREGATIONS = "aggregations",
  WEIGHTS = "weights",
  SETTINGS = "settings",
  OTHERS = "others"
}

export enum WIDGET_CONFIGURATION_PARENT_KEYS {
  SETTINGS = "parent_settings"
}

export const WIDGET_FILTER_TAB_ORDER = "filter_tab_order";
