export type LevelopsTableReportColumnType = {
  dataIndex: string;
  defaultValue: string;
  editable: boolean;
  id: string;
  index: number;
  inputType: string;
  options: Array<any>;
  readOnly: boolean;
  required: boolean;
  title: string;
  key?: string;
  hidden?: boolean;
};
