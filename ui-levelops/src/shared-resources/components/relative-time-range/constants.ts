export enum RelativeTimeRangeUnits {
  TODAY = "today",
  DAYS = "days",
  WEEKS = "weeks",
  MONTHS = "months",
  QUARTERS = "quarters"
}

export enum TimeRangeFilterType {
  ABSOLUTE = "absolute",
  RELATIVE = "relative"
}
